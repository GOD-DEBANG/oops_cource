package Properties_of_opps.Inharitance.Types_of_inharitance.Multiple_inharitance;

public class Not_in_JAVA {
    //If two or more Parent classes has the same variable or properties the the child class will get confused which one to pick
    // Hence the Multiple inheritance is not present in JAVA.
    //We can Access a child class which has more than one Parent By INTERFACE
    //INTERFACES defines what a class does instead of its values
    public static void main(String[] args) {
        System.out.println("Multiple inheritance is Allowed present in JAVA.");
        System.out.println("We can Access a child class which has more than one Parent By INTERFACE");
        System.out.println("INTERFACES defines what a class does instead of its values");
    }
}
